# 贝斯练习RPG

## 项目概况

纯前端单文件 HTML 网站（`index.html`），像素 RPG 风格的贝斯练习工具。数据存储在浏览器 localStorage。

**设计风格**：像素 RPG（星露谷物语风格）— 直角边框、像素字体（Press Start 2P / Silkscreen）、4px 立体按钮、step() CSS 动画。

**核心功能**：日历打卡、练琴计划（7章折叠教学）、练琴统计（RPG六维属性面板）、节拍器（Web Audio API）。

## 开发约束

- 单文件架构，所有 HTML/CSS/JS 在 `index.html` 里
- 纯前端，无构建工具，直接浏览器打开使用
- 修改时保持像素 RPG 风格一致性
- 不可见字符/空格调整、摆造型这类改动除了导致合并冲突之外没有意义。不要做。
- 除非我明确要求，不要引入新文件/新目录，保持单文件结构
